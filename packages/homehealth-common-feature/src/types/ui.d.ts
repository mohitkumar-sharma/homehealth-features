export interface UIProps {
  /* as a boolean of loading */
  isLoading?: boolean;
  /* as a boolean of internet connection */
  isInternetConnected?: boolean;
  /* as a boolean of splash loading */
  isSplashLoading?: boolean;
}
